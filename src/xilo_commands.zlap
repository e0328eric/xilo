#@xilo | A simple replacement of "rm" command {
    *FILES : strings | files or directories to remove;
    -recursive,r := @false | remove directory recursively;
    -force,f := @false | force to delete files/directories;
    -permanent,p := @false | empty the trashbin if FILES is empty. Otherwise, delete contents unrecoverably;
    -show_space,s := @false | show the trashbin space;
}
